{
  "name": "players-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "Codecta academy players fut lambda.",
  "main": "function.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./handler.js --bundle --outfile=./build/index.js",
    "build-prod": "esbuild ./handler.js --bundle --minify --platform=node --outfile=./build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.433.0",
    "@aws-sdk/lib-dynamodb": "^3.433.0",
    "jest": "^29.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
